//arrays->here arrays are dynamic in js
// .. aisa data structure hota hi jo ki ek se jyada element rakh skta hai

// var arr = [1, 2, 3, 4, 5, 6, 7];
// const ans = arr.map((ele) => (ele > 5 ? ele + 5 : ele));

// var nums = [1, 3, 5, 7, 2];
// const ans = nums.filter((ele) => ele > 4);
